# 机房运维监控平台开发计划

**基线日期：** 2026-07-30  
**代码仓库：** https://github.com/hangwei-sun/brmyunwei  
**当前阶段：** 单节点安全开发版，可用于本机联调，不具备生产上线条件。

## 1. 当前进度

| 模块 | 已完成 | 仍缺失 | 完成度判断 |
| --- | --- | --- | --- |
| Web 控制台 | 总览、资产列表/详情、指标趋势、事件处置、规则与通知页面骨架、登录页、加载/错误/空状态 | 真实规则 CRUD、联系人、维护窗口、用户管理、端到端权限状态 | 约 60% |
| 中心端 API | SQLite、资产、指标、事件、规则、通知策略、审计、腾讯云短信适配、本地账号、Bearer、RBAC、Agent Key | 探测调度、告警状态机、通知队列、数据保留、备份、正式迁移 | 约 55% |
| Windows Agent | 上报契约和服务端认证已存在 | 客户端、Windows Service、采集器、证书、安装包、资源测试 | 约 10% |
| 主备高可用 | API 明确报告单节点，不再伪造切换 | 复制、租约、仲裁、切换、回放、演练 | 约 5% |
| 部署与运维 | 开发启动、部署原则和上线门禁文档 | 自包含发布、Windows Service、HTTPS、DPAPI、备份恢复 | 约 30% |
| 自动化测试 | 前端 5 项、后端安全集成测试 4 项、Release 构建 | 业务规则、告警引擎、浏览器 E2E、Agent 兼容与压力测试 | 约 25% |

当前版本可以演示并验证安全 API，但不能接入生产服务器、真实短信联系人或作为双机系统部署。

## 2. Agent 分工和模型策略

最多同时运行 3 个子 Agent，加上主 Agent 共 4 个并发槽位。Agent 必须使用独立文件边界或 Git worktree，禁止多人同时修改同一文件。

| Agent | 推荐模型 | 责任范围 | 使用原则 |
| --- | --- | --- | --- |
| 主 Agent | `gpt-5.6-sol` | 架构决策、任务拆分、代码整合、最终审查、上线判断 | 只处理跨模块和高风险决策 |
| 后端安全 Agent | `gpt-5.6-sol` | 认证、授权、告警状态机、数据一致性、主备安全 | 安全和一致性问题才使用高成本模型 |
| 前端 Agent | `gpt-5.6-terra` medium | React 页面、API 接入、状态管理、交互和前端测试 | 常规产品开发使用经济模型 |
| Agent/QA Agent | `gpt-5.6-terra` medium | Windows 采集端、安装包、测试矩阵、部署文档 | 独立模块和机械验证不使用高成本模型 |

已经完成的并行工作：

- 后端安全 Agent：Bearer 登录、Admin/Operator/Viewer、Agent Key、安全测试和依赖修复。
- 前端 Agent：真实 API 状态、错误处理、指标趋势、代码分包和前端测试。
- 主 Agent：前后端认证整合、菜单精简、真实登录联调和总体计划。

## 3. 开发波次

### 波次 A：单节点可用版（P0，预计 1 至 2 个迭代）

目标：一台 Windows 11 运维电脑可以安全运行中心端，对少量测试目标执行真实监控，但不发生产短信。

- 后端：实现 ICMP、TCP、HTTP/HTTPS 检查调度，带并发上限、超时、抖动和退避。
- 后端：实现事件状态机，包含连续失败确认、恢复确认、事件指纹、冷却和抖动抑制。
- 后端：把规则、通知策略和检查项从演示数据改成完整数据库模型与 CRUD。
- 前端：接入真实规则、检查项、通知策略和用户管理 API；按角色隐藏或禁用操作。
- 数据：增加 EF Core Migration、指标索引、过期清理和数据库备份任务。
- 部署：中心端自包含发布，作为 Windows Service 运行；前端静态文件由同一服务托管。

完成标准：本机安装后无需 Node/.NET SDK；重启电脑后服务自动启动；3 至 5 台测试目标连续运行 72 小时，无重复事件和无界数据增长。

### 波次 B：轻量 Windows Agent（P0，预计 2 个迭代）

目标：Windows Server 2012/2012 R2/Windows 11 可安装一个无入站端口的只读 Agent。

- 优先实现 CPU、内存、磁盘空间、网卡、启动时间和指定 Windows 服务。
- Agent 只主动出站 HTTPS，上报带主机身份、序列号、采集时间和启动 ID。
- 增加短期一次性注册令牌；当前静态 Agent Key 仅作为开发过渡，生产升级为 mTLS。
- 增加有上限的离线缓存、指数退避、滚动日志和时钟偏差检查。
- 提供签名 MSI、静默安装、升级、卸载和回滚流程；不得要求服务器重启。

完成标准：真实 Server 2012 连续运行 7 天；CPU 一分钟平均低于 0.2%，工作集不高于 25 MB，网络低于 50 KB/分钟，业务服务指标无劣化。

### 波次 C：通知闭环（P0，预计 1 个迭代）

目标：严重事件能够稳定、可审计地发送腾讯云短信。

- 实现联系人组、值班时间、服务器组和事件类型匹配。
- 实现待发送队列、退避重试、发送结果、恢复通知和最大重复次数。
- 使用确定性事件指纹去重；批量故障合并为组级事件，避免短信风暴。
- SecretId/SecretKey 仅通过 DPAPI 或受保护环境注入；日志隐藏密钥和手机号。
- 真实号码前先使用测试模板和测试联系人完成失败、限频、超时演练。

完成标准：告警、重复提醒、恢复、静默、维护窗口和 API 失败场景均有自动测试及审计记录。

### 波次 D：主备和生产门禁（P1，预计 2 至 3 个迭代）

目标：两台运维电脑形成可验证的活动/待命系统。

- 先确定配置/事件复制方案，不复制正在写入的 SQLite 文件，不使用网络共享盘承载 WAL。
- 实现节点 mTLS、心跳、配置版本、复制延迟和恢复回放。
- 实现唯一通知租约；若没有第三见证，明确接受网络分区下重复短信风险。
- 执行主节点断电、服务停止、网络分区、恢复同步和回切演练。
- 增加加密备份、异机保存和恢复演练。

完成标准：主节点失联后备节点在目标时间内接管；恢复后事件与审计一致；没有未经授权的双节点写入。

## 4. 当前任务清单

### P0：下一批立即开发

- [ ] 前端按角色控制资产、规则、通知和事件处置按钮。
- [ ] 前端规则、通知策略和用户页面接入真实 API，删除剩余静态业务数据。
- [ ] 后端 ICMP/TCP/HTTP 检查模型、调度器和测试。
- [ ] 后端告警状态机、事件指纹、恢复逻辑和测试。
- [ ] Windows Agent 工程骨架、基础采集器和双目标上报。
- [ ] 中心端自托管前端静态文件，并完成 Windows Service 发布配置。
- [ ] EF Migration、数据清理、备份和容量测试。

### P1：P0 稳定后开发

- [ ] mTLS 注册、证书轮换和 MSI 签名。
- [ ] 腾讯云通知队列、联系人和值班计划。
- [ ] 主备复制、通知租约和外部见证。
- [ ] UPS/PDU/IPMI/SNMP 扩展接口。

### 明确不进入首版

- 远程重启、关机、进程结束或任意脚本执行。
- 软件分发、补丁管理、远程桌面和完整日志平台。
- 数据库业务 SQL 检查和自动修复。
- 公网直接暴露管理面板。

## 5. 测试和上线门禁

每个 PR 必须通过对应模块构建、自动测试和依赖漏洞扫描。生产候选版本还必须满足：

- 前端、API、Agent 均从干净环境构建，0 个高危依赖。
- 所有管理 API 均要求认证和服务端授权；浏览器隐藏按钮不能替代授权。
- HTTPS、证书 ACL、服务账号、密钥保护和管理网防火墙完成检查。
- 3 至 5 台非关键服务器先静默灰度 7 天，再按每批最多 10 台扩展。
- Agent 安装、升级、卸载无需重启服务器且不修改业务服务。
- 告警去重、短信限频、数据清理、备份恢复和故障切换均有演练记录。

任何资源超限、业务性能劣化、明文传输、重复/漏报、数据无界增长或备份失败，都必须停止扩大部署范围。

## 6. GitHub 协作规范

- `main` 只保存构建和测试通过的集成版本，禁止直接上传密钥、数据库、构建产物和生产配置。
- 推荐分支：`feat/backend-probes`、`feat/frontend-management`、`feat/windows-agent`、`test/release-gates`。
- 多 Agent 并行时使用独立 Git worktree；如果共享同一工作区，必须按目录分工并由主 Agent 统一提交。
- PR 必须写明改动范围、接口变化、测试结果、部署影响和回滚方法。
- 高风险模块使用 `gpt-5.6-sol` 审查；普通页面、测试和文档优先使用 `gpt-5.6-terra`。

