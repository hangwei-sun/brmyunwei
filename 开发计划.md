# 单节点隔离预发布计划

更新时间：2026-07-30

## 决策

当前阶段只完成一台控制端和 1 至 2 台非关键服务器的隔离预发布。主备复制、仲裁、自动故障转移和生产评审全部暂缓。只有静默期证据满足门禁后，才决定是否启动主备设计。

## 已完成

- 中心端认证、RBAC、审计、Agent Key 绑定、序列防重放和输入校验。
- 真实 ICMP/TCP/HTTP(S) 探测，以及失败/恢复状态机、事件指纹和去重。
- Agent 扩展采集、失联/重启/指定服务/指标异常事件闭环。
- 通知策略完整 CRUD、服务器组匹配、联系人组、发送状态、重试和重复提醒。
- 前端真实接口、角色可见性、资产分组、规则连续次数和通知策略编辑。
- Windows Service 能力、自包含发布、静态前端托管、数据保留、在线备份和恢复脚本。
- 自动化门禁：前端 9 项、后端 10 项、Agent 自测、Release 构建和漏洞扫描。

## 预发布执行

| 阶段 | 工作 | 完成证据 |
| --- | --- | --- |
| P0 | 生成 `0.2.0-rc.1` 自包含包 | SHA-256 清单、Git commit、CI 全绿 |
| P1 | Windows 11 控制端初始化 | HTTPS 健康检查、一次性管理员登录、管理端匿名访问为 401 |
| P2 | 添加 1 至 2 台非关键服务器 | 资产组、Agent Key、只读 Agent、无新增入站端口 |
| P3 | 静默运行 3 至 7 天 | 不启用真实短信；每日记录心跳、事件、误报和资源曲线 |
| P4 | 压力与恢复 | 探测并发、失败退避、在线备份、停机恢复、通知去重均有记录 |
| P5 | 评审 | 决定继续单节点、补修或进入主备设计 |

## 门禁

- Agent 3 分钟及 24 小时抽样：平均 CPU `<= 0.2%`，工作集 `<= 25 MiB`。
- 默认每分钟一个不超过 16 KiB 的遥测体，含 HTTPS 开销后网络 `<= 50 KiB/分钟`。
- 只监控白名单 Windows 服务；不枚举进程、不执行命令、不修改业务服务。
- 8 并发探测下控制端无请求堆积；故障目标按指数退避，不形成探测风暴。
- 同一事件和通知策略只存在一条发送状态；失败重试不新建重复事件。
- 在线备份通过 `PRAGMA integrity_check` 和 SHA-256；停机恢复后健康检查与数据计数一致。
- 静默期不得出现业务响应劣化、Agent 超预算、明文传输、异常监听、漏报或事件风暴。

## 暂缓项

- 两节点复制、见证/仲裁和唯一通知租约。
- Agent 一次性注册、mTLS、代码签名 MSI 和自动升级。
- 真实短信联系人和生产腾讯云密钥。
- 生产证书、正式域名和面向全机房的防火墙策略。
