# 机房运维监控平台安装手册

本手册面向现场运维人员。安装服务器时**不需要打开 PowerShell、不需要复制命令**：双击安装包，在开始菜单打开配置向导，按本手册填写即可。

本版按生产双机运行设计，需要三台独立 Windows 主机：

| 机器 | 用途 | 必须安装的文件 |
| --- | --- | --- |
| 控制端 A | 平时运行监控、告警和通知 | `MonitoringPlatform-Control-版本号-x64.msi` |
| 控制端 B | 备用控制端，A 故障后受 Witness 许可接管 | 同一个控制端 MSI |
| Witness | 第三方见证，只保存主备租约，不保存监控数据 | `MonitoringPlatform-Witness-版本号-x64.msi` |

被监控的 Windows Server 2012 / 2012 R2 / Windows 11 服务器只安装 `MonitoringPlatform-Agent-版本号-x64.msi`。

## 1. 先准备

请先由网络、证书或域管理员准备以下内容。没有准备好时，不要开始安装。

1. 三台不同的 Windows 主机。Witness 不可与任一控制端共机、共磁盘；建议控制端使用 Windows 11 或较新的 Windows Server。
2. 三个固定内网 IP，例如 `10.10.10.11`、`10.10.10.12`、`10.10.10.13`。不需要 DNS，也不需要购买公网证书。
3. 一名知道三个 IP 的安装人员，以及一条至少 12 位的“证书包口令”。安装向导会自动生成内部根证书、控制端/Witness HTTPS 证书和 Agent 注册 CA；不需要事先准备 PFX 或 CA。
6. 两个受控共享目录，例如 `\\fileserver\monitoring-replication` 与 `\\fileserver\monitoring-keys`。只授权控制端 A、B 的计算机账户和管理员读写；不要给普通用户权限。前者保存快照，后者保存加密的数据保护密钥。
7. 内部防火墙已批准：Agent 仅可出站访问控制端 HTTPS；控制端 A/B 可访问 Witness HTTPS 和共享目录；无需在 Agent 上开入站端口。
8. Agent 服务器已安装 .NET Framework 4.8。Windows Server 2012 / 2012 R2 还需 Windows Management Framework 5.1。

发布人员会交付一个文件夹，至少包括以下 4 个文件：

```text
MonitoringPlatform-Control-版本号-x64.msi
MonitoringPlatform-Witness-版本号-x64.msi
MonitoringPlatform-Agent-版本号-x64.msi
SHA256SUMS
```

请从单位共享目录或 U 盘取得安装包，并核对发布人员提供的文件校验值。不要从聊天工具转发不明 MSI。内网本地签名包可能显示“未知发布者”；请核对发布人员提供的签名人指纹后再确认安装。单位已部署签名证书或使用受管代码签名证书时，不会出现该提示。

## 2. 安装顺序

必须按以下顺序操作：先生成内网证书包，再 Witness，再控制端 A，再控制端 B，最后安装 Agent。控制端 B 在看到 A 的同步和 ready 状态正常前，不会作为可写节点使用。

### 第零步：自动生成内网证书包

1. 在控制端 A 双击控制端 MSI 完成安装，先不要打开“Configure Monitoring Control”。
2. 打开“开始”菜单，选择 **Monitoring Platform > Prepare LAN Certificates**。
3. 填写控制端 A、控制端 B、Witness 的内网 IP，设置一次证书包口令，点击“生成内网证书包”。
4. 将生成目录中的 `LAN-Root.cer` 通过 Windows 双击导入向导导入 A、B、Witness 的“本地计算机 / 受信任的根证书颁发机构”。
5. 将 `Control-A.pfx` 导入 A 的“本地计算机 / 个人”，`Control-B.pfx` 导入 B，`Witness.pfx` 导入 Witness；将 `DataProtection.pfx` 和 `Agent-Issuer.pfx` 都导入 A、B 的“本地计算机 / 个人”，同时将 `Agent-Issuer.cer` 导入 A、B 的“本地计算机 / 受信任的根证书颁发机构”。所有导入都使用同一证书包口令。

全程只使用 Windows 的证书导入界面，不输入命令。证书包及口令只交给安装人员，导入完成后保存在单位密码管理器中。

### 第一步：安装 Witness

1. 在 Witness 主机双击 `MonitoringPlatform-Witness-版本号-x64.msi`，按 Windows 提示完成安装。
2. 打开“开始”菜单，选择 **Monitoring Platform > Configure Monitoring Witness**。
3. 在向导填写 Witness 的 HTTPS 地址，例如 `https://10.10.10.13:9444`，选择刚导入的 Witness HTTPS 证书。
4. 填写控制端 A、控制端 B 的节点名称，例如 `monitor-a`、`monitor-b`。
5. 为 A、B 各准备一条不同的高强度 Witness 密钥，长度 32 至 256 位。用单位密码管理器生成并保管；不要把两条密钥写在聊天、便签或 Excel 中。
6. 点击“完成配置并启动见证服务”。出现 Windows 管理员确认是正常现象。看到“Witness 已启动”后，分别把 A、B 对应的密钥交给控制端安装人员。

Witness 只保存租约和递增 epoch。不要卸载或清空 Witness 数据，否则旧控制端可能无法安全判断租约历史。

### 第二步：安装控制端 A

1. 在控制端 A 双击 `MonitoringPlatform-Control-版本号-x64.msi`，完成安装。
2. 打开“开始”菜单，选择 **Monitoring Platform > Configure Monitoring Control**。
3. 按下表填写。地址必须与准备阶段的 HTTPS 地址完全一致。

| 字段 | 控制端 A 填写内容 |
| --- | --- |
| 本机节点名称 | `monitor-a`，或已与 Witness 约定的名称 |
| 节点角色 | `主用节点 (A)` |
| 本机 HTTPS 地址 | `https://10.10.10.11:8443` |
| 对端节点名称 | `monitor-b` |
| 对端 HTTPS 地址 | `https://10.10.10.12:8443` |
| 见证服务 HTTPS 地址 | `https://10.10.10.13:9444` |
| 共享快照目录 | `\\fileserver\monitoring-replication` |
| 共享密钥目录 | `\\fileserver\monitoring-keys` |
| HTTPS 与密钥证书 | 选择刚导入的 `Control-A.pfx` 对应证书 |
| 共享数据保护证书 | 选择刚导入的 `DataProtection.pfx` 对应证书 |
| Agent 注册 CA 证书 | 选择刚导入的 `Agent-Issuer.pfx` 对应证书 |
| 首次管理员账号、密码 | 设置平台第一个管理员账号；密码至少 12 位 |
| 本节点 Witness 密钥 | 填 Witness 中 `monitor-a` 对应的那一条 |

4. 点击“完成配置并启动”。向导会请求管理员权限、创建 Windows 服务，并只在首次启动期间使用管理员密码。该密码不会写入 JSON 配置文件。
5. 浏览器打开 A 的 HTTPS 地址，使用刚设置的管理员账号登录。进入“设置”完成站点名称、腾讯云短信和通知策略；短信先保持 `disabled` 或 `test`，不要直接启用真实号码。

### 第三步：安装控制端 B

1. 在控制端 B 安装同一个 `MonitoringPlatform-Control-版本号-x64.msi`，再从开始菜单打开控制端配置向导。
2. 所有共享项必须与 A 一致：共享快照目录、共享密钥目录、共享数据保护证书、Agent 注册 CA 证书、Witness 地址。HTTPS 证书选择 B 自己导入的 `Control-B.pfx` 对应证书。
3. 角色选择 `备用节点 (B)`；本机名称填写 `monitor-b`；本机和对端 HTTPS 地址与 A 相反；Witness 密钥使用 `monitor-b` 对应的那一条。
4. 首次管理员账号和密码可以使用与 A 相同的受控值。B 从 A 回放数据后，两端账号会一致。
5. 启动完成后，在 A 的顶部“双节点”状态中查看 B。B 显示 ready、复制持续推进后，才可进行主备演练。

> 不要通过网页按钮直接把 B 改为主用。页面的节点入口仅用于切换管理地址；真正的角色变化必须经过 Witness 租约和受控切换流程。

## 3. 控制台首次设置

登录控制端 A 后，按以下顺序完成：

1. 在“设置”填写机房/站点名称和描述。
2. 在“设置 > 通知”填写腾讯云短信参数。先选择 `test`，只放入测试号码，确认模板、失败重试和去重正确后再切换 `live`。
3. 在“用户”创建第二名管理员或值班人员；不要长期共用首次管理员。
4. 在“资产”添加服务器，分配服务器组、机房和业务系统。
5. 给每个服务器组创建通知策略：可选择腾讯云短信或站内信、告警级别、联系人组、重复周期及是否启用。未设置策略的分组不会发送短信。
6. 为每台待接入服务器创建一次性安装码。安装码只显示一次、有效期很短；使用后立即失效。

## 4. 安装服务器 Agent

每台被监控服务器按以下操作。Agent 只采集只读指标并主动出站上传，不开监听端口、不执行远程命令、不修改业务服务。

1. 在控制台中先添加对应资产，再生成该资产的一次性安装码。
2. 将 `MonitoringPlatform-Agent-版本号-x64.msi` 复制到服务器，双击安装。Windows 提示安装权限时确认即可。
3. 打开“开始”菜单，选择 **Monitoring Platform > Configure Monitoring Agent**。
4. 填写服务器名称、控制端 A HTTPS 地址、控制端 B HTTPS 地址、一次性安装码；可选填写需要观察的 Windows 服务名，例如 `W3SVC,MSSQLSERVER`。Agent 会首次读取并固定两台控制端的证书指纹，无需在每台服务器导入根证书。
5. 点击“Connect and start Agent”。管理员窗口完成证书注册后会自动关闭；看到“Agent connected and running”即完成。
6. 返回控制台确认该资产在线、心跳时间刷新且服务状态正常。

一次性安装码不会保存在 Agent 配置文件，也不会作为 MSI 参数或命令行参数传递。若注册失败，不要反复使用同一安装码；在控制台撤销该资产证书后生成新的安装码再试。

## 5. 上线前验证与回滚

先接入 1 至 2 台非关键服务器，静默运行 3 至 7 天。期间不启用真实联系人短信，记录 Agent CPU、内存、网络、探测超时、误报/漏报、业务延迟、备份和恢复结果。

出现以下任一情况，立即停止扩大范围并卸载当前批次 Agent：

- Agent 平均 CPU 超过 `0.2%`、工作集超过 `25 MiB`、网络超过 `50 KiB/分钟`。
- 业务延迟、磁盘 I/O、服务稳定性变差，或发现异常监听端口。
- 误报、漏报、重复短信、事件无法恢复闭环。
- 在线备份、SQLite 校验、恢复回放或主备复制失败。

双机现场演练必须覆盖：手动计划切换、A 死机/断网后 B 自动接管、A/B 网络分区、旧 A 回放、回切。演练细节见 [deployment/HA-双机生产切换与回放.md](./deployment/HA-双机生产切换与回放.md)。未完成这些演练前，不得把双机标记为“已生产高可用”。

## 6. 升级和卸载

升级时使用同一组件的新 MSI 直接双击安装，Windows Installer 会完成升级；不要手工删除 `Program Files` 下的文件。

卸载 Agent 后会移除 Agent 服务和程序文件，保留 `ProgramData` 下的诊断数据，便于排查。确认业务恢复、控制台离线状态正确后，再由管理员按数据保留策略清理。

控制端或 Witness 的卸载前必须先完成切换与备份。尤其不要在没有确认当前主用节点和 Witness 租约的情况下卸载主用控制端。

## 7. 给发布人员的说明

现场人员不运行构建命令。发布人员在干净的源码工作区使用 [deployment/Publish-InstallerPackages.ps1](./deployment/Publish-InstallerPackages.ps1) 生成本地签名的三种 MSI，并把校验文件一起交付。脚本会编译控制端、Witness、Agent 与配置向导，签名 EXE、脚本和 MSI；内网测试可继续使用现有本地代码签名证书，正式扩大部署前应改用单位受管签名证书。

安装包并不替代现场验收。证书信任、共享目录权限、Windows Server 2012/2012 R2 的实际安装、断网缓存、资源预算、通知去重、备份恢复和 HA 切换仍需留下证据。
