# 单节点隔离预发布部署手册

本手册只用于一台 Windows 11 控制端和 1 至 2 台非关键 Windows 服务器。预发布默认关闭真实短信，不连接关键业务服务器，不代表生产上线完成。

## 1. 发布与校验

在源码目录执行：

```powershell
.\deployment\Publish-Release.ps1 -Version 0.2.0-rc.7
```

解压 `artifacts\monitoring-platform-0.2.0-rc.7-win-x64.zip`，先核对 `release.json`、`SHA256SUMS` 和 Git commit。发布包包含：

```text
app\                         中心端自包含程序和前端
agent\                       Windows Server 2012/2012 R2 Agent
Initialize-IsolatedPrerelease.ps1
Start-IsolatedPrerelease.ps1
Get-IsolatedPrereleaseCredential.ps1
Install-Service.ps1          正式提权注册中心服务时使用
Restore-Database.ps1
```

## 2. 初始化隔离控制端

普通用户可在解压目录执行：

```powershell
.\Initialize-IsolatedPrerelease.ps1
```

初始化会先检查 HTTPS 端口是否已被监听，或被 Windows TCP 排除端口范围保留；检查失败时不会创建安装目录或证书。若默认 `8443` 被占用或保留，先在变更/防火墙审批中确定一个仅供两台灰度服务器使用的端口，再执行：

```powershell
.\Initialize-IsolatedPrerelease.ps1 -HttpsPort 9443
```

后续所有命令、防火墙规则和 Agent `-PrimaryEndpoint` 必须使用同一个已批准端口；本文以下示例以默认 `8443` 为例。

脚本只修改当前用户范围：

- 程序和数据位于 `%LOCALAPPDATA%\MonitoringPlatform-Prerelease`。
- 生成只用于本次预发布的本地根 CA，并签发包含 `localhost` 和本机名的 90 天 RSA/SHA-256 服务器证书。
- 服务器证书写入 `CurrentUser\My`，根 CA 公钥写入 `CurrentUser\Root`；根 CA 私钥不可导出。
- 一次性管理员密码用当前用户 DPAPI 保护，不写入 JSON 或 Git。
- 数据库、Data Protection Key、备份和 Agent 包使用独立目录。

启动：

```powershell
.\Start-IsolatedPrerelease.ps1
```

浏览器访问 `https://localhost:8443`。凭据查看：

```powershell
.\Get-IsolatedPrereleaseCredential.ps1
```

首次登录后在“用户”中立即重设管理员密码。中心端仅在数据库没有任何本地账号时执行引导创建；不存在生产默认密码。

验收：

```powershell
Invoke-RestMethod https://localhost:8443/api/health
```

期望 `status=healthy`、`database=connected`。匿名请求 `/api/hosts` 必须返回 401。

## 3. 管理员提权步骤

当前开发会话不是管理员，以下步骤必须由运维在批准窗口使用提升的 PowerShell 执行：

1. 只允许两台灰度服务器访问控制端 TCP 8443；不要开放任意来源。
2. 将 `%LOCALAPPDATA%\MonitoringPlatform-Prerelease\prerelease-root-ca.cer` 导入两台灰度服务器的 `LocalMachine\Root`。不要把 `prerelease-server.cer` 当作根证书导入；这套证书只用于本次隔离预发布。
3. 若需要控制端随系统启动，使用正式发布模板和 `Install-Service.ps1` 注册 `MonitoringPlatform` 服务；服务身份为虚拟低权限账号，不使用管理员账号。
4. 预发布结束后移除临时防火墙规则和两台服务器上的预发布证书。

防火墙规则必须显式填写两台测试服务器 IP，例如：

```powershell
New-NetFirewallRule -DisplayName 'Monitoring prerelease 8443' -Direction Inbound -Protocol TCP -LocalPort 8443 -RemoteAddress 10.10.9.21,10.10.9.22 -Action Allow
```

## 4. 控制台配置

1. 在“资产”中添加服务器，填写固定主机名、管理 IP、服务器组、机房和业务系统。
2. 为每台服务器生成独立 Agent Key；不要复用或通过普通聊天工具传递。
3. 只添加必要探测：ICMP、关键 TCP 端口或只读 HTTP(S) 健康地址。
4. 探测间隔不低于 10 秒，初始建议 30 秒；超时 1 至 3 秒，失败阈值至少 3 次。
5. 告警规则按每分钟 Agent 样本设置连续次数。CPU 5 次代表约 5 分钟。
6. 通知策略配置服务器组、严重级别、联系人组和重复周期，但静默期保持腾讯云短信 `Enabled=false`。

联系人和腾讯云密钥只通过受保护生产配置或服务环境注入：

```text
TencentCloudSms__SecretId
TencentCloudSms__SecretKey
NotificationContacts__Groups__值班组__0
```

不得写入仓库、前端或审计日志。

## 5. 安装两台灰度 Agent

前提：Windows Server 2012/2012 R2 已安装 .NET Framework 4.8，且可出站访问控制端 HTTPS 8443。不要新增被控机入站规则。

使用提升的 PowerShell 在 Agent 发布目录执行。Key 会以安全提示框输入，不放入命令历史：

```powershell
$key = Read-Host 'Agent Key' -AsSecureString
.\Install-Agent.ps1 -AgentName 'UAT-WEB-01' -AgentKey $key `
  -PrimaryEndpoint 'https://CONTROL-HOST:8443/api/v1/agents/ingest' `
  -WatchedServices 'W3SVC','MSSQLSERVER'
```

脚本结构化写入配置，使用 `LOCAL SERVICE`、设置严格 ACL 并启动服务。确认没有新监听端口、计划任务、驱动、远程命令或业务服务变更。

## 6. 静默期测量

持续 3 至 7 天，不发送真实短信。每天记录：Agent 在线率、误报、探测超时、事件数量、控制端磁盘增长和业务负责人反馈。

Agent 启动至少 3 分钟后执行：

```powershell
.\Measure-AgentResource.ps1 -DurationSeconds 180
```

硬门禁：平均 CPU `<=0.2%`、工作集 `<=25 MiB`。Agent 自测已限制最坏 32 服务遥测体不超过 16 KiB；默认一分钟上传一次，网络预算为 `<=50 KiB/分钟`。

控制端探测压力从 4 并发开始，逐步到 8；同时观察 CPU、内存、HTTP 响应和 SQLite 写入。不得直接跳到 64 并发。

## 7. 备份、恢复和告警去重

管理员可调用 `POST /api/maintenance/backup` 立即创建在线备份。后台每天按配置自动执行，备份经过 SQLite 完整性检查并生成 `.sha256`。

恢复演练必须在停止中心端后进行：

```powershell
Stop-Service MonitoringPlatform
.\Restore-Database.ps1 -BackupPath 'C:\ProgramData\MonitoringPlatform\backup\monitoring-YYYYMMDD-HHMMSS.db'
Start-Service MonitoringPlatform
```

隔离进程模式则先停止 `Start-IsolatedPrerelease.ps1`，使用对应 `%LOCALAPPDATA%` 数据库路径。恢复后检查健康、资产数、规则数、事件数和审计记录。

去重验收：同一探测连续失败只产生一个未恢复事件；同一事件与通知策略只产生一条发送状态；恢复后关闭原事件，再次故障才允许新一轮通知。自动化测试已重复执行规划器并验证记录数不增长，静默期还需观察真实故障序列。

## 8. 停止条件与评审

出现任一情况立即停止扩大范围：

- Agent 超过 CPU、内存或网络预算。
- 业务响应、磁盘 I/O 或服务稳定性劣化。
- HTTPS 校验失败后降级明文，或出现异常监听端口。
- 同一故障形成事件风暴、重复通知状态或恢复不闭环。
- 在线备份、校验或停机恢复失败。
- 控制端数据、日志或离线队列无上限增长。

静默期结束后形成一页评审记录：两台服务器型号/系统、Agent 资源数据、网络量、探测并发、事件准确率、备份恢复耗时、去重结果和停止条件。证据通过后再决定是否启动主备设计和生产上线评审。
