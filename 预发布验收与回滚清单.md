# 单节点预发布验收与回滚清单

本清单只适用于当前单节点、隔离预发布。主备复制、仲裁/见证、唯一通知租约、故障切换与回放均不属于本次验收范围。

## 执行顺序

1. 在控制端使用管理员 Token 执行 `deployment\Invoke-PrereleaseAcceptance.ps1`。`-ObservedNetworkKiBPerMinute` 必须来自按进程或五元组采集的真实网络证据，不能用网卡总流量替代。`-BusinessHealthUri` 必须是业务负责人批准的只读健康检查地址。
2. 先不传 `-RunSmsTemplateTest`，确认所有真实联系人策略关闭。配置腾讯云测试模板和测试号码后，才传入该开关、测试号码与模板参数。任何失败都不允许改用真实联系人。
3. 创建受控的失败投递后，以 `-RequireRetryObservation` 重跑。验收脚本会检查同一 `incidentId + policyId` 没有重复状态，并要求看到失败记录的重试；不得以生产故障制造测试事件。
4. 在每台 Windows Server 2012/2012 R2 灰度机确认 .NET Framework 4.8 与 Windows PowerShell 5.1 后，用提升 PowerShell 运行 `deployment\Test-WindowsAgentPrerelease.ps1 -ApprovedRootCertificateThumbprint <根CA指纹>`。检查运行时、服务身份、HTTPS、根证书链和离线队列目录。
5. 在批准窗口仅阻断该 Agent 到控制端的出站 HTTPS，持续两个采样周期；恢复网络后等待上传，再执行 `-Mode CaptureOfflineCache`。不要关闭整台服务器网卡，也不要影响业务端口。
6. 保留每次 JSON 输出、业务延迟样本、网络抓包摘要和短信控制台回执。静默期 3 至 7 天每天执行一次资源/业务检查。

示例（令牌只在当前 PowerShell 会话内存中存在）：

```powershell
$token = Read-Host 'Admin access token' -AsSecureString
.\deployment\Invoke-PrereleaseAcceptance.ps1 `
  -ControlUrl 'https://CONTROL-HOST:8443' -BearerToken $token `
  -AgentComputerName 'UAT-WEB-01' -ObservedNetworkKiBPerMinute 18.4 `
  -BusinessHealthUri 'https://UAT-WEB-01/health' `
  -OutputPath .\evidence\uat-web-01.json
```

## 硬停止条件

出现任一项立即停止扩大范围，移除当前批次 Agent 或按变更方案回滚：

- 平均 CPU 超过 `0.2%`、工作集超过 `25 MiB`、真实网络流量超过 `50 KiB/分钟`。
- 业务健康检查失败或响应超过批准阈值。
- 发现新监听端口、TLS 信任失败、明文降级，或 Agent 服务异常重启。
- 事件/通知重复、错误重试不工作、误报或漏报。
- 离线缓存未能形成或恢复上传无法验证。

## 卸载回滚

先保存 JSON、日志和待上传样本；经变更审批后执行：

```powershell
.\Test-WindowsAgentPrerelease.ps1 -Mode Rollback -ConfirmRollback `
  -MsiPath .\MonitoringPlatform.Agent-0.3.0-x64.msi `
  -ApprovedSignerThumbprint $signer
```

该命令调用已发布包内的卸载程序，移除服务和程序文件，保留 `%ProgramData%\MonitoringPlatform\Agent`，并将该目录复制到回滚证据目录。确认服务已消失、未新增监听端口、业务健康检查恢复正常后，才可在批准窗口手工清理保留的数据和临时根证书。
